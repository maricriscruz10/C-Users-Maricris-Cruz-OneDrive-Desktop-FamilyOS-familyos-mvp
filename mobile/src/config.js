// Point this at your computer's LAN IP (not "localhost") when testing on a
// physical phone with Expo Go — the phone is a separate device on the
// network and "localhost" would refer to the phone itself.
// Find your IP: Mac/Linux `ipconfig getifaddr en0` or `hostname -I`,
// Windows `ipconfig` (look for IPv4 Address).
// Example: export const API_BASE = 'http://192.168.1.42:4000';

export const API_BASE = 'http://192.168.1.42:4000'; // <-- CHANGE THIS
