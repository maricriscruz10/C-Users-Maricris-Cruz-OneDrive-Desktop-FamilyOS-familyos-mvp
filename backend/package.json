{
  "name": "familyos-backend",
  "version": "1.0.0",
  "private": true,
  "description": "FamilyOS MVP backend - zero-dependency Node.js (http + node:sqlite + crypto). No npm install required.",
  "main": "server.js",
  "scripts": {
    "seed": "node seed.js",
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
